# 프론트 공개 설정 예시
# cp .env.example .env 후 값 수정

# Vite dev server가 /api를 Spring Boot(8080)로 프록시
VITE_API_BASE_URL=/api

# "enabled" 시 MSW 브라우저 모킹 활성화 (백엔드 없이 개발 시 사용)
# .env.development에서 자동으로 enabled로 설정됨
VITE_API_MOCKING=
