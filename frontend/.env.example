# Vite dev server가 /api를 Spring Boot(8080)로 프록시하므로 보통 /api 그대로 사용
VITE_API_BASE_URL=/api

# "enabled"로 설정하면 MSW 브라우저 모킹이 활성화된다 (백엔드 없이 개발 시 사용)
# .env.development에서 자동으로 enabled로 설정되어 있으므로 개발 환경에서는 별도 설정 불필요
VITE_API_MOCKING=
